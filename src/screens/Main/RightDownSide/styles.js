import styled from "styled-components";
import { Color, Font } from "~themes";

import {
  DefaultArea,
  DefaultBox,
  DefaultBar,
  DefaultText,
  DefaultBarTitle,
  DefaultBarIconWrapper,
  DefaultBarIconContainer,
  DefaultContent,
} from "../styles";

export const Area4 = styled(DefaultArea)``;
export const Box = styled(DefaultBox)`
  padding: 10px 20px;
  display: flex;
  align-items: flex-end;
  justify-content: flex-end;
`;
export const CopyrightText = styled(DefaultText)`
  font-weight: 100;
`;
